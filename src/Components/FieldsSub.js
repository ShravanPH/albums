import React from 'react'
import { Typography, Grid, Button } from '@material-ui/core'
import { useState } from 'react'

function FieldsSub(props)
{
    // console.log("name = ",props.values.name)
    // console.log("password = ",props.values.password)
    // console.log("email = ",props.values.email)
    return(
        <form >

            {/* <Typography color={props.props.color.PROPS.L_D_Mode} >IN FIELDS SUB</Typography>

             <input type="submit"  className="inSub" />   */}
        </form>

    )

}export default FieldsSub